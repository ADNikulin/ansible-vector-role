---
# this is example playbook to execute Ansible tests

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: example assertion
      assert:
        that: true

- name: "service started"
  hosts: all
  gather_facts: false
  tasks:
    - name: 
      assert:
        that: true
